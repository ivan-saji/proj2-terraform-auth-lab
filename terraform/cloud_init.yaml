#cloud-config

package_update: true

write_files:
  - path: /tmp/cloudinit-test.txt
    permissions: '0644'
    content: |
      =================================
      Cloud-init is working!
      VM created successfully.
      =================================

runcmd:
  - echo "Cloud-init execution completed." >> /tmp/cloudinit-test.txt